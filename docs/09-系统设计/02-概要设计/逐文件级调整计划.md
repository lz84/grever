# Nexus 逐文件级模块调整计划

**版本**: v0.1
**日期**: 2026-05-27
**依据**: 核心技术架构.md + 模块组件调研报告.md
**目标**: 从"扁平路由 160+ 文件 + 集中调度"→ "独立模块 + 消息总线"

---

## 当前代码结构总览

```
packages/server/src/reins/
├── (root)/              20 files, 268KB    ← 高度碎片化
├── api/                 164 files, 916KB   ← 严重碎片化，需要重组
├── grasp/               3 files, 38KB      ← 基本合理
├── manager/             7 files, 37KB      ← 空壳居多
├── migration/           12 files, 56KB     ← 需要整理
├── models/              15 files, 90KB     ← 基本合理
├── persistence/         16 files, 68KB     ← 基本合理
├── scheduler/           15 files, 234KB    ← 需要拆分大文件
├── services/            9 files, 68KB      ← 需要按领域归类
└── verifiers/           4 files, 35KB      ← 需要合并到 scheduler
```

---

## Phase 1：基础设施（1-2 周）

### 1.1 日志引擎（Log Engine）

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `reins/logging/__init__.py` | 日志引擎入口 |
| **新建** | `reins/logging/engine.py` | LogEngine 实现（基于 Loguru） |
| **新建** | `reins/logging/schema.py` | 统一日志 Schema + trace_id 透传 |
| **新建** | `reins/logging/queries.py` | 日志查询接口 |
| **新建** | `requirements.txt` | 添加 `loguru` 依赖 |
| **修改** | `reins/api/server.py` | 初始化日志引擎 |
| **修改** | ALL 文件 | 逐步替换 print/logging → LogEngine |

**日志引擎目录结构**：
```
reins/logging/
├── __init__.py
├── engine.py          # LogEngine 核心
├── schema.py          # 统一 Schema
├── queries.py         # 查询接口
└── middleware.py      # FastAPI 中间件（自动注入 trace_id）
```

### 1.2 Command Bus 基础

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `reins/messaging/__init__.py` | 消息总线入口 |
| **新建** | `reins/messaging/command.py` | Command/CommandBus 基础类 |
| **新建** | `reins/messaging/event.py` | Event/EventBus 基础类（已有 event_bus.py，重构） |
| **迁移** | `reins/event_bus.py` → `reins/messaging/event_legacy.py` | 保留兼容 |
| **迁移** | `reins/events.py` → `reins/messaging/events_legacy.py` | 保留兼容 |

**消息总线目录结构**：
```
reins/messaging/
├── __init__.py
├── command.py         # Command + CommandBus
├── event.py           # Event + EventBus (基于 Redis)
└── types.py           # 核心 Command/Event 类型定义
```

### 1.3 清理空壳/碎片文件

| 操作 | 文件 | 说明 |
|------|------|------|
| **删除** | `reins/api/main.py` (0KB) | 空文件 |
| **删除** | `reins/api/artifacts.py` (0KB) | 空文件 |
| **删除** | `reins/api/assignment.py` (0KB) | 空文件 |
| **删除** | `reins/api/attachments_router.py` (0KB) | 空文件 |
| **删除** | `reins/api/dag_conversation.py` (0KB) | 空文件 |
| **删除** | `reins/api/dag_conversation_response.py` (0KB) | 空文件 |
| **删除** | `reins/api/dispute_logic.py` (0KB) | 空文件 |
| **删除** | `reins/api/goal_decompose.py` (0KB) | 空文件 |
| **删除** | `reins/api/goals.py` (0KB) | 空文件 |
| **删除** | `reins/api/goals_exploration.py` (0KB) | 空文件 |
| **删除** | `reins/api/goals_exploration_models.py` (0KB) | 空文件 |
| **删除** | `reins/api/goals_models.py` (0KB) | 空文件 |
| **删除** | `reins/api/grasp_router.py` (0KB) | 空文件 |
| **删除** | `reins/api/human_review.py` (0KB) | 空文件 |
| **删除** | `reins/api/knowledge_injector.py` (0KB) | 空文件 |
| **删除** | `reins/api/load_manager.py` (0KB) | 空文件 |
| **删除** | `reins/api/projects.py` (0KB) | 空文件 |
| **删除** | `reins/api/scenario_emergency.py` (0KB) | 空文件 |
| **删除** | `reins/api/settings.py` (0KB) | 空文件 |
| **删除** | `reins/api/settings_models.py` (0KB) | 空文件 |
| **删除** | `reins/api/solutions.py` (0KB) | 空文件 |
| **删除** | `reins/api/workflows_logic.py` (0KB) | 空文件 |
| **删除** | `reins/api/api_documentation.py` (0KB) | 空文件 |
| **删除** | `reins/api/artifacts_helpers.py` (0KB) | 空文件 |
| **删除** | `reins/api/assignment_context.py` (0KB) | 空文件 |
| **删除** | `reins/api/assignment_endpoints.py` (0KB) | 空文件 |
| **删除** | `reins/api/workflow_edit_logic.py` (0KB) | 空文件 |

**小文件合并**（< 1KB 且无独立逻辑）：

| 合并目标 | 来源文件 | 说明 |
|---------|---------|------|
| `tasks_helpers.py` | 吸收 `tasks_models.py` | 模型 + 辅助函数放一起 |
| `scenarios_crud.py` | 吸收 `scenario_models.py` | 场景模型 + CRUD 放一起 |
| `goals_crud.py` | 吸收 `goals_list.py` + `goals_decompose.py` | 目标 CRUD 集中 |
| `projects_crud.py` | 吸收 `projects_list.py` | 项目 CRUD 集中 |
| `agents_router.py` | 吸收 `_agents_models.py` | Agent 路由 + 模型放一起 |

---

## Phase 2：模块解耦（2-4 周）

### 2.1 日志引擎落地

按 1.1 的文件列表执行。

### 2.2 大文件拆分

#### result_verifier.py (59KB) → 拆为 4 个文件

| 新文件 | 来源代码段 | 预估大小 |
|--------|-----------|---------|
| `reins/scheduler/verification/rules.py` | 验证规则解析 + 类型定义 | 15KB |
| `reins/scheduler/verification/engine.py` | _run_verifier_checks + 各类型验证执行 | 20KB |
| `reins/scheduler/verification/arbitration.py` | 仲裁触发 + 争议处理 | 12KB |
| `reins/scheduler/verification/reporter.py` | 验证报告生成 + comment 写入 | 12KB |
| `reins/scheduler/verification/__init__.py` | 聚合入口 | 1KB |

#### project_executor.py (35KB) → 拆为 3 个文件

| 新文件 | 来源代码段 | 预估大小 |
|--------|-----------|---------|
| `reins/scheduler/executor/orchestrator.py` | tick 循环 + 孤儿恢复 + 状态管理 | 15KB |
| `reins/scheduler/executor/dispatch_coordinator.py` | 任务派发协调 + Agent 交互 | 12KB |
| `reins/scheduler/executor/result_collector.py` | 结果收集 + 进度更新 | 8KB |
| `reins/scheduler/executor/__init__.py` | 聚合入口 | 1KB |

#### scenarios_crud.py (40KB) → 拆为 3 个文件

| 新文件 | 来源代码段 | 预估大小 |
|--------|-----------|---------|
| `reins/api/scenarios/crud.py` | 场景 CRUD 核心逻辑 | 15KB |
| `reins/api/scenarios/derive.py` | 场景派生（已有 scenarios_derive.py 合并） | 13KB |
| `reins/api/scenarios/custom.py` | 自定义场景（已有 scenarios_custom.py 合并） | 12KB |
| `reins/api/scenarios/__init__.py` | 聚合入口 + router | 2KB |

#### background_tasks.py (24KB) + tracker.py (24KB) + tracker_sync.py (26KB)

| 新文件 | 来源 | 预估大小 |
|--------|------|---------|
| `reins/tracking/tracker.py` | tracker.py 精简 | 15KB |
| `reins/tracking/sync.py` | tracker_sync.py 精简 | 12KB |
| `reins/tracking/__init__.py` | 聚合入口 | 1KB |

### 2.3 API 路由重组（164 → ~40 个文件）

**按领域分组**：

```
reins/api/
├── __init__.py              # 聚合所有路由
├── server.py                # FastAPI 入口（保持不变）
│
├── goals/                   # 目标管理（~15 个文件 → 5 个）
│   ├── __init__.py
│   ├── crud.py              # CRUD 操作（合并 goals_crud + goals_list + goals_models）
│   ├── decompose.py         # 分解逻辑（合并 goal_decompose* + goals_decompose）
│   ├── exploration.py       # 探索模式（合并 goals_exploration*）
│   └── router.py            # 路由注册
│
├── projects/                # 项目管理（~7 个文件 → 4 个）
│   ├── __init__.py
│   ├── crud.py              # CRUD（合并 projects_crud + projects_list）
│   ├── workflow.py          # 工作流（合并 projects_workflow + projects_verifier_lifecycle）
│   └── router.py
│
├── tasks/                   # 任务管理（~15 个文件 → 7 个）
│   ├── __init__.py
│   ├── crud.py              # CRUD（合并 tasks_crud + tasks_models + tasks_list）
│   ├── execution.py         # 执行（合并 tasks_execution + tasks_execution_core + tasks_execution_endpoints）
│   ├── lifecycle.py         # 生命周期（合并 tasks_lifecycle + tasks_state）
│   ├── human_input.py       # HITL（合并 tasks_human_input + tasks_review）
│   ├── helpers.py           # 辅助函数（合并 tasks_helpers）
│   └── router.py
│
├── agents/                  # Agent 管理（~10 个文件 → 5 个）
│   ├── __init__.py
│   ├── registry.py          # 注册/心跳（合并 _agents_register + _agents_heartbeat + agent_health_probe）
│   ├── management.py        # 管理（合并 _agents_manage + _agents_discover + admin）
│   ├── matching.py          # 匹配（合并 agent_matching + load_manager*）
│   └── router.py
│
├── scenarios/               # 场景库（~12 个文件 → 5 个）
│   ├── __init__.py
│   ├── crud.py              # CRUD（合并 scenarios_crud + scenario_models）
│   ├── instantiate.py       # 实例化（合并 scenario_instantiate + scenario_instantiate_v2 + scenario_create）
│   ├── match.py             # 匹配（合并 scenario_match + scenario_workflow）
│   └── router.py
│
├── human_input/             # 人机协同（~9 个文件 → 4 个）
│   ├── __init__.py
│   ├── queries.py           # 查询（合并 human_input_queries + human_input_actions）
│   ├── review.py            # 裁决（合并 human_review* + human_review_stats）
│   └── router.py
│
├── workflows/               # 工作流（~12 个文件 → 5 个）
│   ├── __init__.py
│   ├── dag.py               # DAG 逻辑（合并 workflow_dag* + workflow_edit*）
│   ├── diagram.py           # 可视化（合并 workflow_diagram + workflows）
│   ├── split.py             # 拆分（合并 workflow_split）
│   └── router.py
│
├── solutions/               # 方案管理（~9 个文件 → 4 个）
│   ├── __init__.py
│   ├── crud.py              # CRUD（合并 solutions_crud + solutions_helpers）
│   ├── iteration.py         # 迭代（合并 solutions_iteration + solutions_consensus + solutions_discussion）
│   └── router.py
│
├── industry/                # 行业标签 + 行业包（~4 个文件 → 3 个）
│   ├── __init__.py
│   ├── tags.py              # 标签（合并 industry_tags_routes + industry_tag_models）
│   ├── packs.py             # 行业包（合并 industry_packs_routes）
│   └── router.py
│
├── extensions/              # 系统扩展（~15 个文件 → 5 个）
│   ├── __init__.py
│   ├── mcp.py               # MCP（合并 mcp*）
│   ├── skills.py            # 技能（合并 skills.py）
│   ├── attachments.py       # 附件（合并 attachments + attachment_helpers）
│   ├── artifacts.py         # 产出物（合并 artifacts*）
│   └── router.py
│
├── security/                # 安全（~4 个文件 → 3 个）
│   ├── __init__.py
│   ├── core.py              # 核心（合并 security.py）
│   ├── audit.py             # 审计（合并 audit.py + traces_router）
│   └── router.py
│
├── system/                  # 系统配置（~6 个文件 → 3 个）
│   ├── __init__.py
│   ├── settings.py          # 设置（合并 settings*）
│   ├── dashboard.py         # 仪表盘（合并 dashboard_stats）
│   └── router.py
│
├── dispute/                 # 争议管理（~3 个文件 → 2 个）
│   ├── __init__.py
│   ├── manage.py            # 管理（合并 dispute_manage + _dispute_logic_helpers）
│   └── router.py
│
├── inject/                  # 知识注入（~5 个文件 → 2 个）
│   ├── __init__.py
│   ├── core.py              # 核心（合并 inject + knowledge_injector*）
│   └── router.py
│
├── grasp/                   # 认知引擎（已有，保持）
│   ├── caller.py
│   ├── circuit_breaker.py
│   └── fallback.py
│
├── dag/                     # DAG 对话（~6 个文件 → 2 个）
│   ├── __init__.py
│   ├── conversation.py      # 对话（合并 dag_conversation*）
│   └── router.py
│
├── scheduler/               # 调度（合并 scheduler_router）
│   └── router.py
│
├── search/                  # 搜索（保持）
│   └── router.py
│
├── workspace/               # 工作区（保持）
│   └── router.py
│
├── reports/                 # 报告（保持）
│   └── router.py
│
├── error_codes.py           # 错误码（保持）
├── error_handler.py         # 错误处理（保持）
└── app_state.py             # 应用状态（保持）
```

**重组后文件数：~70 个**（从 164 减少 57%）

### 2.4 scheduler/ 目录重组

```
reins/scheduler/
├── __init__.py
├── core.py                  # 调度核心（保持不变）
├── dependency_resolver.py   # 依赖解析（保持不变）
├── health_manager.py        # 健康管理（保持不变）
├── stats.py                 # 统计（保持不变）
│
├── executor/                # 执行器（从 project_executor.py 拆分）
│   ├── __init__.py
│   ├── orchestrator.py
│   ├── dispatch_coordinator.py
│   └── result_collector.py
│
├── verification/            # 验证器（从 result_verifier.py 拆分）
│   ├── __init__.py
│   ├── rules.py
│   ├── engine.py
│   ├── arbitration.py
│   └── reporter.py
│
├── assigner/                # 任务派发（从 task_assigner.py 重构）
│   ├── __init__.py
│   ├── matcher.py           # 匹配逻辑
│   ├── load_balancer.py     # 负载计算
│   └── dispatcher.py        # 派发逻辑
│
├── recoverer/               # 任务恢复（从 task_recoverer.py 精简）
│   └── __init__.py
│
├── runner/                  # 任务运行（从 task_runner.py 精简）
│   └── __init__.py
│
├── timeout/                 # 超时管理（从 timeout_scheduler.py + services/timeout_handler.py 合并）
│   ├── __init__.py
│   ├── scheduler.py
│   └── handler.py
│
├── context/                 # 任务上下文（从 task_context_builder.py 精简）
│   └── __init__.py
│
└── heartbeat/               # 心跳决策（从 heartbeat_decision.py 精简）
    └── __init__.py
```

### 2.5 services/ 按领域归类

| 来源 | 迁移到 | 说明 |
|------|--------|------|
| `services/agent_matcher.py` | `scheduler/assigner/matcher.py` | 合并到匹配引擎 |
| `services/auto_tagging.py` | `industry/tags.py` | 合并到行业标签 |
| `services/tag_prerequisites.py` | `industry/tags.py` | 合并到行业标签 |
| `services/task_validator.py` | `scheduler/assigner/dispatcher.py` | 合并到派发 |
| `services/feishu_notification.py` | `human_input/queries.py` | 合并到人机协同 |
| `services/goal_decomposition.py` | `goals/decompose.py` | 合并到目标分解 |
| `services/llm_service.py` | `grasp/llm.py` | 移到认知引擎 |
| `services/workspace_manager.py` | `api/workspace/` | 合并到工作区 |
| `services/timeout_handler.py` | `scheduler/timeout/handler.py` | 合并到超时管理 |

### 2.6 verifiers/ 合并到 scheduler

| 来源 | 迁移到 | 说明 |
|------|--------|------|
| `verifiers/ability_tracker.py` | `scheduler/verification/reporter.py` | 合并到验证报告 |
| `verifiers/agent_dispatcher.py` | `scheduler/assigner/dispatcher.py` | 合并到派发 |
| `verifiers/evidence_parser.py` | `scheduler/verification/rules.py` | 合并到验证规则 |
| `verifiers/task_builders.py` | `goals/decompose.py` | 合并到目标分解 |

### 2.7 manager/ 空壳清理

| 文件 | 操作 | 说明 |
|------|------|------|
| `manager/dispute_manager.py` (0KB) | 删除 | 空壳，功能在 `dispute/manage.py` |
| `manager/goal_manager.py` (0KB) | 删除 | 空壳，功能在 `goals/crud.py` |
| `manager/project_manager.py` (0KB) | 删除 | 空壳，功能在 `projects/crud.py` |
| `manager/agent_discovery.py` (5KB) | 合并到 `agents/management.py` | 功能重复 |
| `manager/agent_registry.py` (22KB) | 合并到 `agents/registry.py` | 与 api 重复 |
| `manager/task_manager.py` (5KB) | 合并到 `tasks/crud.py` | 功能重复 |
| `manager/base.py` (1KB) | 删除 | 空壳基类 |

**manager/ 目录整体删除**，功能合并到各自领域。

### 2.8 状态机引入 transitions 库

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `requirements.txt` | 添加 `transitions` 依赖 |
| **修改** | `reins/models/task.py` | 引入 transitions 管理 Task 状态机 |
| **修改** | `reins/models/goal.py` | 引入 transitions 管理 Goal 状态机 |
| **修改** | `reins/models/goal_statemachine.py` | 用 transitions 重写 |
| **删除** | `reins/state_machine.py` (11KB) | 功能被 transitions 替代 |

---

## Phase 3：消息总线完善（4-6 周）

### 3.1 核心 Command 实现

| Command | Handler | 迁移来源 |
|---------|---------|---------|
| `AssignTaskCommand` | `scheduler/assigner/dispatcher.py` | `task_assigner.py` |
| `VerifyResultCommand` | `scheduler/verification/engine.py` | `result_verifier.py` |
| `InstantiateScenarioCommand` | `scenarios/instantiate.py` | `scenario_instantiate.py` |
| `DecomposeGoalCommand` | `goals/decompose.py` | `goal_decompose*.py` |
| `TriggerHITLCommand` | `human_input/queries.py` | `human_input_actions.py` |
| `RulingCommand` | `human_input/review.py` | `human_review*.py` |

### 3.2 核心 Event 实现

| Event | Listener | 迁移来源 |
|-------|---------|---------|
| `TaskAssignedEvent` | LogEngine, AgentSDK | `events.py` |
| `TaskCompletedEvent` | Verifier, ProjectExecutor | `events.py` |
| `TaskFailedEvent` | TaskRecoverer, LogEngine | `events.py` |
| `AgentHeartbeatEvent` | HealthManager, LoadCalculator | `events.py` |
| `ScenarioInstantiatedEvent` | LogEngine, ScenarioLibrary | `events.py` |

### 3.3 Redis Event Bus 升级

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `reins/messaging/redis_event_bus.py` | 基于 Redis Pub/Sub 的事件总线 |
| **修改** | `reins/event_bus.py` | 适配新接口，保持向后兼容 |
| **新建** | `requirements.txt` | 添加 `redis` 依赖 |

---

## Phase 4：Vigil + Evo 落地（6-8 周）

### 4.1 Vigil 安全域

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `reins/vigil/__init__.py` | Vigil 入口 |
| **新建** | `reins/vigil/trust.py` | 信任评估引擎 |
| **新建** | `reins/vigil/audit.py` | 审计日志 |
| **新建** | `reins/vigil/alerts.py` | 告警管理 |
| **新建** | `reins/vigil/access.py` | RBAC 访问控制 |
| **新建** | `reins/vigil/router.py` | API 路由 |
| **迁移** | `reins/security.py` → `reins/vigil/legacy_security.py` | 保留兼容 |

### 4.2 Evo 进化域

| 操作 | 文件 | 说明 |
|------|------|------|
| **新建** | `reins/evo/__init__.py` | Evo 入口 |
| **新建** | `reins/evo/distiller.py` | 技能提炼器 |
| **新建** | `reins/evo/solidify.py` | 能力固化器 |
| **新建** | `reins/evo/mutation.py` | 变异引擎 |
| **新建** | `reins/evo/analyzer.py` | 分析器 |
| **新建** | `reins/evo/a2a.py` | A2A 通信 |
| **新建** | `reins/evo/router.py` | API 路由 |

---

## 文件变更统计

| 操作 | 数量 | 说明 |
|------|------|------|
| **新建** | ~45 个文件 | 新模块、新目录 |
| **合并** | ~80 个文件 → ~40 个文件 | API 路由重组 |
| **拆分** | 4 个大文件 → ~12 个文件 | result_verifier/project_executor/scenarios_crud/background_tasks |
| **删除** | ~40 个文件 | 空壳文件 + 重复文件 + 过时代码 |
| **移动** | ~20 个文件 | services/verifiers/manager 归入各自领域 |
| **总计** | 246 → ~190 个文件 | **减少 23%** |

---

## 执行顺序建议

| 步骤 | 内容 | 风险 | 预计时间 |
|------|------|------|---------|
| 1 | Phase 1: 日志引擎 + Command Bus + 清理空文件 | 低 | 1 周 |
| 2.1 | Phase 2.1: API 路由重组（goals/projects/tasks） | 中 | 1 周 |
| 2.2 | Phase 2.2: API 路由重组（agents/scenarios/workflows） | 中 | 1 周 |
| 2.3 | Phase 2.3: scheduler/ 目录重组 + 大文件拆分 | 高 | 1 周 |
| 2.4 | Phase 2.4: services/verifiers/manager 清理 | 低 | 3 天 |
| 2.5 | Phase 2.5: transitions 状态机引入 | 中 | 3 天 |
| 3 | Phase 3: 消息总线完善 | 高 | 2 周 |
| 4 | Phase 4: Vigil + Evo 落地 | 高 | 6 周 |

---

*文档结束*
